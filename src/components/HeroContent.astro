---
import { Image } from "astro:assets";
import pfp from "../../public/pfp.webp"
---
<div data-aos="fade-up" class="mx-auto  py-4 sm:py-4 lg:py-4">
    <div class="w-40 text-center mx-auto">
      <Image class="rounded-full text-center" src={pfp} alt="Javier Picón" loading="eager" />
    </div>
    <div class="text-center">
      <h1 class="py-4 tracking-tight text-gray-300">
        Hola! Soy Javier Picón 👋
      </h1>
      <p class="text-3xl font-bold text-secondary text-transparent bg-clip-text bg-gradient-to-r from-secondary to-[#585858] sm:text-3xl md:text-5xl pb-2">
      Desarrollador Web
      </p>
      <p class="w-5/6 mx-auto mt-2 text-gray-100 font-medium">Me dedico a programar sobre todo en el backend y un poco en el frontend.</p>
      <div class="container_mouse">
        <a href="#about" aria-label="scrollButton" class="mouse-btn">
            <span class="mouse-scroll"></span>
        </a>
    </div>
    </div>
  </div>
  <style is:inline>
    :root {
  --color: #087ec4;
}

body {
  background-color: #212121e8;
}

.container_mouse {
  z-index: 50;
  text-align: center;
  color: var(--color);
  font-weight: bold;
  text-transform: uppercase;
  padding-top: 2rem;
  cursor: pointer;
}

.container_mouse .mouse-btn {
  margin: auto;
  width: 20px;
  height: 40px;
  border: 2px solid var(--color);
  border-radius: 30px;
  display: flex;
}

.container_mouse .mouse-btn .mouse-scroll {
  display: block;
  width: 10px;
  height: 10px;
  background: linear-gradient(170deg, #087ec4, #14A1F0);
  border-radius: 50%;
  margin: auto;
  animation: scrolling 1s linear infinite;
}


@keyframes scrolling {
  0% {
    opacity: 0;
    transform: translateY(-20px);
  }

  100% {
    opacity: 1;
    transform: translateY(10px);
  }
}
  </style>